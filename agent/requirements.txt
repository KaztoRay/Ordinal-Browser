# ============================================================
# OrdinalV8 — Python 보안 에이전트 의존성
# ============================================================

# gRPC 통신 (C++ ↔ Python IPC)
grpcio>=1.60
grpcio-tools>=1.60
protobuf>=4.25

# OpenAI GPT-4 API
openai>=1.10

# 딥러닝 / 로컬 모델
torch>=2.1
transformers>=4.36
sentence-transformers>=2.3

# 데이터 검증 / 설정
pydantic>=2.5
pydantic-settings>=2.1

# 비동기 HTTP 클라이언트
aiohttp>=3.9

# 비동기 SQLite
aiosqlite>=0.19

# 수치 연산 / ML
numpy>=1.26
scikit-learn>=1.3

# 토큰 카운팅
tiktoken>=0.5

# 테스트
pytest>=7.4
pytest-asyncio>=0.23
